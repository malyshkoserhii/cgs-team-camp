import React from 'react';
import { ResetForm } from '~shared/components/auth/reset-form';

const ResetPassPageContainer: React.FunctionComponent = () => <ResetForm />;

export default ResetPassPageContainer;
